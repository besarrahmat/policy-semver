# @policy-semver/core

Classify, bump, changelog, and fail-closed `versioning.config.json` for PolicySemVer.

Install: `pnpm add @policy-semver/core@0.1.0`

The GitHub Action is **not** this package — pin `uses: besarrahmat/policy-semver@<sha>` (prod) or `@v0.1.0` (demo).
