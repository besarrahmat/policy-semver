# examples/* are standalone consumer stubs, not workspace packages.
packages:
  - "packages/*"
allowBuilds:
  esbuild: true
overrides:
  nanoid@^3: 3.3.18
  # tsup@8.5.1 still declares esbuild ^0.27; GHSA-g7r4-m6w7-qqqr is patched in 0.28.1+.
  # Drop when tsup (and any other direct parent) declares ^0.28.
  esbuild: "^0.28.1"
